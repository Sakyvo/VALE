Status: open
Executor: Claude Code

## Parent

`.docs/prd/2026-08-05-blatant-cheater-ingestion-and-sbi-two-tier.md`

## What to build

把剩余约 2151 个投稿包按目标仓库分批入库，目录规模从约 1150 增至约 3900。

归档继续存放于 GitHub 材质包仓库，沿用满仓标记与自动滚动机制——不迁到对象存储。当前编号最大的仓库尚有约 2.76GB 余量，其后约需新建 11 个仓库。每批填满一个仓库（约 5GB、约 200 个包），走完整的四阶段审批，每阶段需要与审阅结果绑定的摘要。分批的意义是把失败的重跑范围限制在一个仓库，而不是 53GB。

三个包超过 GitHub 的 100MB 单文件硬限。规范化的冗余与死路径清理在尺寸闸门之前执行，因此先跑规范化再判定尺寸——本来能救的包不该被误杀。清理后仍超限的记为待处理并保留审计原因，**不为它们新开存储通道**：那需要一次显式的存储路径扩展，是独立议题。

每批完成后跑九张真图的轻量回归。全部完成后跑一次完整重生成、完整回归、合成语料全量评估与规模化性能测量——这是本批工作的最终验收，也是判断两级检索在真实四倍规模下是否达标的唯一依据。

若最终评估显示搜图性能或召回不达标，如实记录并作为独立议题处理，不要在本 issue 内临时调参把测试压绿。

## 实施共识（grill 2026-08-10）

- 输入为 `D:\Blatant Cheater` 全量 2754 个归档（63GB，less/ 233 + more/ 2521），规范化 + 视觉内容指纹自动剔重，预期净入库 ~2201 个；不做预预估调参，重复判定以指纹为准。
- 按 4 个新仓库（packs-007 → packs-010）分 4 批；满仓自动滚动沿用既有机制；不加代码级并发，接受 8-12 小时总时耗、多日多阶段执行。
- 指纹重建范围包含 CLIP embedding 数据（data/sbi-clip-embeddings.bin），但客户端保持 `ENABLE_CLIP = false`——是否启用由 016 尺子实测数据后续决定，不在本轮硬开。
- 入库期展示资产走本地 thumbnails 路径；R2 迁移（012/013 人工验收）与本 issue 并行，不阻塞入库。
- 增量验收：本 issue 各阶段完成即更新状态；最终统一人工验收。

## Acceptance criteria

- [ ] 剩余约 2151 个包按目标仓库分批完成入库，每批走完整四阶段审批且每阶段摘要与审阅结果绑定
- [ ] 仓库容量到达上限时自动标记满仓并滚动到下一个编号，未出现手工指派
- [ ] 三个超过 100MB 的包已先经规范化清理再判定；降至限制以下的正常入库，仍超限的记为待处理并保留审计原因
- [ ] 未为超限包新开任何存储通道
- [ ] 全部入库包进入 `Blatant Cheater` List，未进入自有收藏 List，出现在主页网格与公开索引中
- [ ] 高版本材质、Overlay、视觉重复三类识别在全量下按既有规则生效，结果留有审计记录
- [ ] 全部展示资产已降采样并上传至 R2，母本已落入本地母本目录
- [ ] 每批完成后的九张真图轻量回归全部通过
- [ ] 全量完成后的完整重生成、九张真图组级全中、合成语料全量评估、规模化性能测量四项均已执行并记录
- [ ] 典型搜图下载量与响应时间在四倍规模下的实测结果已记录；若不达标，已如实说明且未通过临时调参掩盖
- [ ] 主仓库不含材质包归档文件，临时中转目录在成功与失败路径上均已清理
- [ ] `npm test` 通过

## Blocked by

- `020-blatant-cheater-pilot.md`

## 放量前置状态（2026-08-14）

- 020 试点已完成 50 包全链路（40 upload_new + 5 conflict retain + 1 duplicate + 4 skip），验证了 upload-folder → backfill-extract → generate-index → build → SBI 重生成链路在 Blatant Cheater List 下端到端可用。
- 全量源：`D:\Blatant Cheater` 2754 个归档（63GB，less/ 233 + more/ 2521），规范+视觉指纹自动剔重，预期净入库 ~2201。
- 全量 dry-run 在 2754 包上做 content fingerprinting 会超时（之前 600s 未写 manifest），需分批 dry-run 或提升并发——这是放量的工程前置。
- 执行计划：按 4 个新仓库（packs-007→010）分 4 批，每批走完整四阶段审批；满仓自动滚动；不加代码级并发；预计 8-12 小时串行上传——超出单次会话自主范围，需人工分批监控执行。
- 复用 020 试点的 duplicate-resolution 模式处理全量中的同名异版/content_duplicate（人工 retain 决策）。
- 管线缺陷（020 记录的 3 项）需在放量前修复：ensureRepo 探测已存在仓库、gh repo clone 绕过 proxy、detect-overlay vips OOM。

剩余验收项（R2 相关 + 九图 + 全量评估 + 性能测量）受 012/013 R2 人工验收与 8-12 小时上传阻塞，按 issue 规则如实记录、不调参。
